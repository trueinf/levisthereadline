# Copy this file to `.env` and paste your OpenAI API key.
# The key is read only by the Vite dev server (see vite.config.ts) and is
# proxied to OpenAI — it is never bundled into the browser.
#
#   cp .env.example .env
#   # then edit .env and restart `npm run dev`

OPENAI_API_KEY=
